# ============================================================
#  deploy/nginx/websocket.conf
#  map لتحديد قيمة Connection header لدعم WebSocket
#  يُضمَّن تلقائياً عبر include في nginx.conf
# ============================================================

map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
